{
  "version": 2,
  "buildCommand": "npm install --legacy-peer-deps --no-audit --no-fund && npm run build || (echo 'Build failed' && exit 1)",
  "outputDirectory": "dist",
  "builds": [
    {
      "src": "dist/app.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/dist/app.js"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
} 