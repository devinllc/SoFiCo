{
  "version": 2,
  "buildCommand": "cd server && npm install --legacy-peer-deps --no-audit --no-fund && npm run build || (echo 'Build failed' && exit 1)",
  "outputDirectory": "server/dist",
  "builds": [
    {
      "src": "server/dist/app.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/server/dist/app.js"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
} 